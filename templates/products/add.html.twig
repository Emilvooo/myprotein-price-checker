{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_4_horizontal_layout.html.twig' %}

{% block body %}
    <div class="row mb-3">
        <div class="col-10">
            <h3 class="text-dark">Add product</h3>
        </div>
        <div class="col-2">
            <a class="btn btn-dark float-right" href="{{ path('products_index') }}" role="button">Back</a>
        </div>
    </div>
    {{ form(form) }}
{% endblock %}